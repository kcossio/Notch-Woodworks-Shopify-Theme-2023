{%- for block in section.blocks -%}
  {%- case block.type -%}

    {%- when 'slider_image' -%}
      {%- # Label: Image -%}
      {%- assign image = block.settings.image -%}
      {{ image | image_url: width: image.width | image_tag: class: 'add_your_class_here', loading: 'lazy' }}

  {%- endcase -%}
{%- endfor -%}

{% schema %}
  {
  "name": "Full-width Image Slider",
  "class": "section image-slider",
  "tag": "section",
  "settings": [],
  "blocks": [
    {
      "name": "Slider Image",
      "type": "slider_image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image",
          "info": "Upload or choose an image to add to the slider"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Full-width Image Slider"
    }
  ]
}  
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}